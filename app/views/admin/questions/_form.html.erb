<%= form_for [:admin, @quiz, @question] do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :reason %>
  <%= form.text_area :reason %>
  <%= form.submit %>
<% end %>
