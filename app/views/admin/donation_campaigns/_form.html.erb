<%= form_for [:admin, @donation_campaign] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :amount %>
  <%= f.text_field :amount %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit "Submit" %>
<% end %>
